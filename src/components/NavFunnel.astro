---
---

<nav class="nav-funnel">
  <a class="brand" href="/">
    <img src="/immagini/dettagli/logo.png" alt="Osteria del Vicario" />
  </a>
  <div class="funnel-actions">
    <a class="btn btn-outline" href="/camere">Prenota una camera</a>
    <a class="btn btn-cotto" href="/tavolo">Prenota un tavolo</a>
  </div>
</nav>

<style>
  .nav-funnel {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.1rem clamp(1.25rem, 4vw, 3rem);
    background: rgba(33, 23, 16, 0.32);
    backdrop-filter: blur(14px) saturate(140%);
    -webkit-backdrop-filter: blur(14px) saturate(140%);
    border-bottom: 1px solid rgba(242, 233, 215, 0.08);
  }

  .brand {
    display: flex;
    align-items: center;
  }

  .brand img {
    height: 42px;
    width: auto;
    filter: drop-shadow(0 1px 3px rgba(0, 0, 0, 0.5));
  }

  .funnel-actions {
    display: flex;
    gap: 0.75rem;
  }

  @media (max-width: 640px) {
    .funnel-actions .btn span {
      display: none;
    }
    .funnel-actions {
      gap: 0.5rem;
    }
    .btn {
      padding: 0.7rem 1rem;
      font-size: 0.85rem;
    }
  }
</style>
