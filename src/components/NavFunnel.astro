---
---

<nav class="nav-funnel">
  <a class="brand" href="/">Osteria del Vicario</a>
  <div class="funnel-actions">
    <a class="btn btn-outline" href="/camere">Prenota una camera</a>
    <a class="btn btn-cotto" href="/tavolo">Prenota un tavolo</a>
  </div>
</nav>

<style>
  .nav-funnel {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem clamp(1.25rem, 4vw, 3rem);
    background: linear-gradient(to bottom, rgba(33, 23, 16, 0.65), transparent);
  }

  .brand {
    font-family: var(--font-display);
    font-size: 1.1rem;
    letter-spacing: 0.02em;
    color: var(--avorio);
  }

  .funnel-actions {
    display: flex;
    gap: 0.75rem;
  }

  @media (max-width: 640px) {
    .funnel-actions .btn span {
      display: none;
    }
    .funnel-actions {
      gap: 0.5rem;
    }
    .btn {
      padding: 0.7rem 1rem;
      font-size: 0.85rem;
    }
  }
</style>
